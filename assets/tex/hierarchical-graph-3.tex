\documentclass{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usetikzlibrary{matrix}
\begin{document}

\begin{tikzpicture}[>=latex]
\matrix[matrix of math nodes, column sep=2em, row sep=2em] (mat)
{
    % & separates nodes and \\ begins new lines
    & & & \mu, \tau^2 \\ 
    & \theta_1 & & \ldots & & \theta_n \\
    y_{1,1} & \ldots & y_{n_1, 1} & \ldots & y_{1, J} & 
    \ldots & y_{n_J, J} \\
    & & & \sigma^2 \\
};

\draw[->] (mat-1-4) -- (mat-2-2);
\draw[->] (mat-1-4) -- (mat-2-6);

\foreach \column in {1, 3}
{
    \draw[->] (mat-2-2) -- (mat-3-\column);
    \draw[<-] (mat-3-\column) -- (mat-4-4);
}

\foreach \column in {5, 7}
{
    \draw[->] (mat-2-6) -- (mat-3-\column);
    \draw[<-] (mat-3-\column) -- (mat-4-4);
}

\node[anchor=east] at ([xshift =-140pt]mat-2-4) 
{$\theta_j \sim \text{N}(\mu, \tau^2)$};
\node[anchor=east] at ([xshift =-140pt]mat-3-4) 
{$y_{i, j} \sim \text{N}(\theta_j, \sigma^2)$};
\end{tikzpicture}
\end{document}
